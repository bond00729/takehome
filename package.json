{
  "name": "takehome",
  "version": "1.0.0",
  "author": "Matthew Bond",
  "description": "Backend takehome assessment",
  "license": "MIT",
  "main": "src/index.ts",
  "scripts": {
    "format": "prettier --write './src/**/*.ts'",
    "lint": "eslint --max-warnings 0 ./src/**/*.ts",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "dev": "docker-compose --profile dev up -d && sleep 1 && $npm_execpath db:push && SERVER_URL=http://localhost:8080 nodemon -r tsconfig-paths/register ./src/index.ts",
    "test": "docker-compose --profile dev up -d && sleep 1 && DATABASE_URL=postgresql://application:password@localhost:5432/test?schema=public $npm_execpath db:push && DATABASE_URL=postgresql://application:password@localhost:5432/test?schema=public PORT=9090 SERVER_URL=http://localhost:9090 LOG_LEVEL=silent mocha --timeout 60000 --exit -r ts-node/register -r tsconfig-paths/register './src/**/*.spec.ts'",
    "test:load": "pnpm start && pnpm dlx artillery run ./artillery/v1.yaml --output ./artillery/reports/v1.json && pnpm dlx artillery report ./artillery/reports/v1.json",
    "build": "tsc",
    "start": "docker-compose --profile app up -d --build && sleep 1 && $npm_execpath db:push"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "2.2.1",
    "@prisma/client": "5.0.0",
    "express": "4.18.2",
    "pino": "8.14.1",
    "pino-http": "8.3.3",
    "swagger-jsdoc": "6.2.8",
    "swagger-ui-express": "5.0.0",
    "tsconfig-paths": "4.2.0",
    "zod": "3.21.4"
  },
  "devDependencies": {
    "@types/chai": "4.3.5",
    "@types/chai-arrays": "2.0.0",
    "@types/express": "4.17.17",
    "@types/mocha": "10.0.1",
    "@types/node": "20.4.2",
    "@types/swagger-jsdoc": "6.0.1",
    "@types/swagger-ui-express": "4.1.3",
    "@typescript-eslint/eslint-plugin": "6.0.0",
    "@typescript-eslint/parser": "6.0.0",
    "chai": "4.3.7",
    "chai-arrays": "2.2.0",
    "chai-http": "4.4.0",
    "eslint": "8.45.0",
    "eslint-config-prettier": "8.8.0",
    "eslint-plugin-unused-imports": "3.0.0",
    "mocha": "10.2.0",
    "nodemon": "3.0.1",
    "pino-pretty": "10.0.1",
    "prettier": "3.0.0",
    "prisma": "5.0.0",
    "ts-node": "10.9.1",
    "typescript": "5.1.6"
  },
  "volta": {
    "node": "18.16.1"
  }
}
